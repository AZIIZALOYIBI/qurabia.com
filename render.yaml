services:
  - type: web
    name: qurabia-backend
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    dockerfilePath: backend/Dockerfile
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
