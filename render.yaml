services:
  - type: web
    name: qurabia-backend
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /health
    dockerfilePath: backend/Dockerfile
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: APP_ENV
        value: "production"
      - key: RATE_LIMIT_DB_PATH
        value: "/tmp/rate_limit.db"
      - key: KEM_MASTER_SEED
        sync: false
      - key: DSA_SIGNING_KEY
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false
